version: "3.9"

services:
  gentle:
    image: lowerquality/gentle
    container_name: gentle
    ports:
      - "8765:8765"

  text-to-video:
    build: .
    container_name: text-to-video
    ports:
      - "8000:8000"
    depends_on:
      - gentle